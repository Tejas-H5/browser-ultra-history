{
  "name": "browser-graph",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build": "tsc && node build-dev.mjs",
    "watch": "tsc && node watch-dev.mjs",
    "check": "tsc",
    "build-prod": "node build-prod.mjs"
  },
  "devDependencies": {
    "esbuild": "0.21.3",
    "typescript": "^5.2.2",
    "webextension-polyfill": "^0.12.0"
  },
  "dependencies": {
    "@types/webextension-polyfill": "^0.10.7"
  }
}
